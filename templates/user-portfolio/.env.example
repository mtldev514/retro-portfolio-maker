# ─── Supabase (required for supabase mode) ────────────────
# Get these from: Supabase Dashboard → Project Settings → API
# Used for both data storage (DB) and file uploads (Storage)
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# ─── Cloudinary (legacy — only needed in local mode) ──────
# Not required if using Supabase mode for file storage.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
